import 'package:flutter/material.dart';

class Operators extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Operators Page')),
      body: Center(child: Text('Welcome to the Operators Page!')),
    );
  }
}